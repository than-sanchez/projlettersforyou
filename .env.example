# Encryption Key (generate a random 32+ character string)
# IMPORTANT: Add this to Replit Secrets instead of .env for better security
ENCRYPTION_KEY=your-encryption-key-here

# Admin Account (default admin user)
# IMPORTANT: Add these to Replit Secrets instead of .env for better security
ADMIN_USERNAME=your-admin-username
ADMIN_PASSWORD=your-admin-password

# Note: Database credentials are no longer needed as the app now uses SQLite
# The database file is located at api/letters.db
